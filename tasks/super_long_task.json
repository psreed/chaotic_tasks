{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Sleeps for a really long time (30mins) in order to force a timeout.",
  "parameters": {
  },
  "implementations" : [
    {"name": "uptime.sh", "requirements": ["shell"]},
    {"name": "uptime.ps1", "requirements": ["powershell"]}
  ]
}